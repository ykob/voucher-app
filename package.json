{
  "name": "voucher-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "turbo-run": "dotenv turbo run --",
    "api": "pnpm turbo-run -F \"api\"",
    "web": "pnpm turbo-run -F \"web\"",
    "db": "pnpm turbo-run -F \"db\"",
    "generate": "pnpm turbo-run generate",
    "migrate": "pnpm turbo-run migrate --env-mode=loose",
    "build": "pnpm turbo-run build",
    "dev": "pnpm turbo-run dev",
    "lint": "pnpm turbo-run lint"
  },
  "author": "Yoichi Kobayashi",
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@9.4.0"
}
