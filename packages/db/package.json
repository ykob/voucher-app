{
  "name": "db",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push --skip-generate",
    "gui": "prisma studio"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1"
  }
}
